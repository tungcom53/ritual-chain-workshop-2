# Result Output

The workshop data is useful, but raw objects are not always nice
to read.

I made a tiny formatter for the script output.

It does not change the market data.

It only changes how I see it in the terminal.

Long questions are shortened so one result does not take over
the whole screen.

I also kept the resolved/open state visible.
